<%- include('layout', { body: null }) %>

<h2>Список студентів</h2>
<a href="/students/create">+ Додати студента</a>
<ul>
  <% students.forEach(student => { %>
    <li><%= student.lastName %> <%= student.firstName %> - група: <%= student.groupName %></li>
  <% }) %>
</ul>
